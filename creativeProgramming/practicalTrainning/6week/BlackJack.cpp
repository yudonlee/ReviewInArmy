#include <iostream>
#include <stdio.h>
#include <ctype.h>
int main(){
    int sum = 0;
    int number;
    cin >> number; 
    int i = 0 ;
    while( i < number){
        char input;
        cin >> input;
        if(isdigit(input))
            
    }
}
